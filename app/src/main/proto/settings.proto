syntax = "proto3";

option java_package = "com.programmersbox.settings";
option java_multiple_files = true;

message PokedexSettings {
    PokedexViewType viewType = 1;
}

enum PokedexViewType {
    Grid = 0;
    List = 1;
}